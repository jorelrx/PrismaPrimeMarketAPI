#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Bloqueio de commit direto na branch main
branch=$(git symbolic-ref --short HEAD 2>/dev/null || echo "")
if [ "$branch" = "main" ]; then
  echo "🚫 Commit direto na branch main é proibido! Use uma feature branch e PR."
  exit 1
fi

exit 0
